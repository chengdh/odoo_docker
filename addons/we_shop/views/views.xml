<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="we_shop.list">
      <field name="name">we_shop list</field>
      <field name="model">we_shop.we_shop</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="we_shop.action_window">
      <field name="name">we_shop window</field>
      <field name="res_model">we_shop.we_shop</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="we_shop.action_server">
      <field name="name">we_shop server</field>
      <field name="model_id" ref="model_we_shop_we_shop"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="we_shop" id="we_shop.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="we_shop.menu_1" parent="we_shop.menu_root"/>
    <menuitem name="Menu 2" id="we_shop.menu_2" parent="we_shop.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="we_shop.menu_1_list" parent="we_shop.menu_1"
              action="we_shop.action_window"/>
    <menuitem name="Server to list" id="we_shop" parent="we_shop.menu_2"
              action="we_shop.action_server"/>
    -->
  </data>
</odoo>